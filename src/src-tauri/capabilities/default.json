{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default desktop capability for the main editor window.",
  "windows": ["main", "document-window-*"],
  "permissions": ["core:default", "core:window:allow-destroy"]
}
